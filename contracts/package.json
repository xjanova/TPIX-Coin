{
  "name": "tpix-contracts",
  "version": "1.0.0",
  "description": "TPIX Chain Smart Contracts",
  "private": true,
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy:wtpix": "npx hardhat run scripts/deploy-wtpix.js --network tpix",
    "deploy:bonding-curve": "npx hardhat run scripts/deploy-bonding-curve.js --network tpix",
    "verify:sources": "node scripts/verify-sources.js",
    "verify:recheck": "node scripts/verify-sources.js --recheck",
    "deploy:noderegistry": "npx hardhat run scripts/deploy-noderegistry.js --network tpix",
    "deploy:noderegistry:dry": "npx hardhat run scripts/deploy-noderegistry.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-chai-matchers": "^2.1.2",
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "@nomicfoundation/hardhat-network-helpers": "^1.1.2",
    "@nomicfoundation/hardhat-toolbox": "^7.0.0",
    "chai": "^4.5.0",
    "ethers": "^6.13.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0"
  }
}
